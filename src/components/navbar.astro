---
const currentYear = new Date().getFullYear();
---

<header class="font-satoshi">
    <!-- Notification Bar -->
    <div id="notification-bar" class="bg-[#344CB7] relative overflow-hidden">
        <div
            class="max-w-7xl mx-auto px-4 py-3 flex items-center justify-between"
        >
            <p class="text-white text-sm font-medium">
                🎉 Welcome to MarineShip! Experience the future of maritime
                services.
            </p>
            <button
                id="notification-dismiss"
                class="text-white hover:text-white/80 transition-colors"
            >
                <span class="sr-only">Dismiss</span>
                <svg class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                    <path
                        d="M4.293 4.293a1 1 0 011.414 0L10 8.586l4.293-4.293a1 1 0 111.414 1.414L11.414 10l4.293 4.293a1 1 0 01-1.414 1.414L10 11.414l-4.293 4.293a1 1 0 01-1.414-1.414L8.586 10 4.293 5.707a1 1 0 010-1.414z"
                    ></path>
                </svg>
            </button>
        </div>
        <!-- Enhanced Animated Background with multiple layers -->
        <div class="absolute inset-0 overflow-hidden pointer-events-none">
            <div
                class="animate-wave absolute inset-0 opacity-10 bg-[radial-gradient(circle_at_50%_120%,rgba(255,255,255,0.5),transparent_65%)]"
            >
            </div>
            <div
                class="animate-wave-delayed absolute inset-0 opacity-10 bg-[radial-gradient(circle_at_30%_150%,rgba(255,255,255,0.5),transparent_65%)]"
            >
            </div>
            <div
                class="animate-float absolute inset-0 opacity-20 bg-[linear-gradient(45deg,rgba(255,255,255,0.2)_25%,transparent_25%,transparent_50%,rgba(255,255,255,0.2)_50%,rgba(255,255,255,0.2)_75%,transparent_75%,transparent)]"
            >
            </div>
        </div>
    </div>

    <!-- Main Navigation -->
    <nav
        class="bg-white/60 backdrop-blur-xl sticky top-0 z-50 border-b border-gray-200/80 transition-all duration-300 transform"
        id="main-nav"
    >
        <div class="max-w-7xl mx-auto">
            <div
                class="flex items-center justify-between h-16 sm:h-20 px-4 sm:px-6 lg:px-8"
            >
                <!-- Logo - Updated for better mobile display -->
                <a
                    href="/"
                    class="flex items-center space-x-3 sm:space-x-4 group"
                >
                    <div class="relative">
                        <div
                            class="absolute inset-0 rounded-xl bg-[#344CB7]/20 blur-2xl group-hover:blur-3xl transition-all duration-500 animate-pulse"
                        >
                        </div>
                        <div
                            class="absolute inset-0 rounded-xl rotate-180 bg-[#344CB7]/20 blur-xl group-hover:blur-2xl transition-all duration-500 animate-pulse delay-100"
                        >
                        </div>
                        <img
                            src="/public/home/logo.jpg"
                            alt="Alcanza ship services"
                            class="relative h-8 w-8 sm:h-10 sm:w-10 transform group-hover:scale-110 transition-transform duration-300"
                        />
                    </div>
                    <div class="overflow-hidden">
                        <h1 class="text-lg sm:text-xl font-bold text-[#344CB7]">
                            Alcanza
                        </h1>
                        <p
                            class="text-[0.6rem] sm:text-[0.65rem] font-medium tracking-widest text-gray-600 transform transition-transform group-hover:translate-x-2"
                        >
                            Ship Services
                        </p>
                    </div>
                </a>

                <!-- Desktop Navigation -->
                <div class="hidden lg:flex lg:items-center lg:space-x-1">
                    <a href="/" class="nav-link group">
                        <span
                            class="group-hover:text-violet-600 transition-colors duration-300"
                            >Home</span
                        >
                        <span class="nav-indicator"></span>
                    </a>
                    <a href="/about" class="nav-link">
                        <span>About</span>
                        <span class="nav-indicator"></span>
                    </a>
                    <a href="/services" class="nav-link">
                        <span>Services</span>
                        <span class="nav-indicator"></span>
                    </a>
                    <a href="/contact" class="nav-link">
                        <span>Contact</span>
                        <span class="nav-indicator"></span>
                    </a>
                </div>

                <!-- Action Buttons -->
                <div class="hidden lg:flex lg:items-center lg:space-x-4">
                    <a href="/quote" class="btn-primary group">
                        <span>Get Started</span>
                        <svg
                            class="ml-2 -mr-1 w-4 h-4 group-hover:translate-x-1 transition-transform duration-300"
                            fill="none"
                            stroke="currentColor"
                            viewBox="0 0 24 24"
                        >
                            <path
                                stroke-linecap="round"
                                stroke-linejoin="round"
                                stroke-width="2"
                                d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
                        </svg>
                    </a>
                </div>

                <!-- Updated Mobile Menu Button -->
                <button
                    type="button"
                    class="lg:hidden mobile-menu-button inline-flex items-center justify-center p-2 rounded-lg text-gray-600 hover:text-violet-600 hover:bg-violet-50 transition-colors duration-200"
                >
                    <span class="sr-only">Open menu</span>
                    <svg
                        class="w-6 h-6"
                        fill="none"
                        stroke="currentColor"
                        viewBox="0 0 24 24"
                    >
                        <path
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            stroke-width="2"
                            d="M4 6h16M4 12h16M4 18h16"></path>
                    </svg>
                </button>
            </div>
        </div>

        <!-- Enhanced Mobile Menu -->
        <div class="hidden mobile-menu lg:hidden">
            <div
                class="px-4 pt-2 pb-3 space-y-2 bg-white/90 backdrop-blur-xl border-t border-gray-200/80"
            >
                <a href="/" class="mobile-link">Home</a>
                <a href="/about" class="mobile-link">About</a>
                <a href="/services" class="mobile-link">Services</a>
                <a href="/contact" class="mobile-link">Contact</a>
                <div class="grid gap-2 pt-4">
                    <a href="/quote" class="mobile-btn-primary">
                        Get Started
                    </a>
                </div>
            </div>
        </div>
    </nav>
</header>

<style>
    @import url("https://api.fontshare.com/v2/css?f[]=satoshi@900,700,500,400&display=swap");

    .nav-link {
        @apply flex items-center px-4 py-2 text-black hover:text-[#344CB7] font-medium relative transition-all duration-300 hover:scale-105;
    }

    .nav-indicator {
        @apply absolute -bottom-px left-0 right-0 h-0.5 bg-[#344CB7] scale-x-0 group-hover:scale-x-100 transition-transform duration-300 origin-left;
    }

    .menu-item {
        @apply flex items-center justify-between p-3 rounded-xl hover:bg-gray-50 transition-colors duration-200;
    }

    .btn-primary {
        @apply inline-flex items-center px-6 py-3 rounded-xl text-sm font-medium text-white 
        bg-[#344CB7]
        hover:bg-[#2a3d92]
        transition-all duration-300 shadow-lg shadow-indigo-600/20 
        hover:shadow-xl hover:shadow-indigo-600/30 
        hover:-translate-y-0.5 hover:scale-105 
        border border-white/20 backdrop-blur-sm;
    }

    .btn-secondary {
        @apply inline-flex items-center px-5 py-2.5 rounded-xl text-sm font-medium 
        text-gray-700 bg-white/80 hover:bg-white 
        transition-all duration-300 hover:shadow-lg 
        hover:-translate-y-0.5 border border-gray-200/50 
        backdrop-blur-sm hover:border-violet-200;
    }

    .mobile-link {
        @apply block px-3 py-2.5 rounded-xl text-base font-medium text-black hover:text-[#344CB7] hover:bg-[#344CB7]/10 transition-colors duration-200;
    }

    .mobile-btn-primary {
        @apply block w-full text-center px-4 py-3 rounded-xl text-sm font-medium text-white 
        bg-[#344CB7]
        hover:bg-[#2a3d92]
        transition-all duration-200 
        shadow-md shadow-indigo-600/10 
        hover:shadow-lg hover:shadow-indigo-600/20;
    }

    .mobile-btn-secondary {
        @apply block w-full text-center px-4 py-3 rounded-xl text-sm font-medium 
        text-gray-700 bg-gray-50 hover:bg-gray-100 
        transition-all duration-200 
        border border-gray-200/50;
    }

    @keyframes wave {
        0% {
            transform: translateY(0) rotate(0deg);
        }
        50% {
            transform: translateY(-7%) rotate(3deg);
        }
        100% {
            transform: translateY(0) rotate(0deg);
        }
    }

    @keyframes wave-delayed {
        0% {
            transform: translateY(0) rotate(0deg);
        }
        50% {
            transform: translateY(-10%) rotate(-3deg);
        }
        100% {
            transform: translateY(0) rotate(0deg);
        }
    }

    @keyframes float {
        0% {
            transform: translateY(0) translateX(0);
        }
        50% {
            transform: translateY(-5px) translateX(5px);
        }
        100% {
            transform: translateY(0) translateX(0);
        }
    }

    .animate-wave {
        animation: wave 12s ease-in-out infinite;
    }

    .animate-wave-delayed {
        animation: wave-delayed 15s ease-in-out infinite;
    }

    .animate-float {
        animation: float 6s ease-in-out infinite;
    }

    /* Enhanced Animations */
    @keyframes gradient {
        0% {
            background-position: 0% 50%;
        }
        50% {
            background-position: 100% 50%;
        }
        100% {
            background-position: 0% 50%;
        }
    }

    .animate-gradient {
        background-size: 200% 200%;
        animation: gradient 8s ease infinite;
    }

    /* Add scroll behavior */
    .nav-scrolled {
        @apply bg-white/90 shadow-lg shadow-gray-900/5;
    }

    /* Updated/Added Mobile Styles */
    .mobile-dropdown-button {
        @apply flex items-center justify-between px-3 py-2 rounded-xl text-base font-medium text-gray-700 hover:text-violet-600 hover:bg-violet-50 transition-colors duration-200 w-full;
    }

    .mobile-sublink {
        @apply block px-3 py-2 rounded-xl text-sm font-medium text-gray-600 hover:text-violet-600 hover:bg-violet-50/50 transition-colors duration-200;
    }

    /* Add new styles */
    .nav-hidden {
        transform: translateY(-100%);
    }

    .nav-visible {
        transform: translateY(0);
    }

    /* Enhanced mobile menu animation */
    .mobile-menu {
        transition: max-height 0.3s ease-in-out;
        max-height: 0;
        overflow: hidden;
    }

    .mobile-menu.active {
        max-height: 400px;
    }
</style>

<script>
    const mobileMenuButton = document.querySelector(".mobile-menu-button");
    const mobileMenu = document.querySelector(".mobile-menu");
    const notificationDismiss = document.querySelector(".notification-dismiss");

    mobileMenuButton?.addEventListener("click", () => {
        mobileMenu?.classList.toggle("hidden");
        mobileMenu?.classList.toggle("active");

        // Animate the menu icon
        const icon = mobileMenuButton.querySelector("svg");
        icon?.classList.toggle("rotate-90");
    });

    notificationDismiss?.addEventListener("click", () => {
        notificationDismiss.closest(".notification-bar")?.remove();
    });

    // Get the notification bar and dismiss button elements
    const notificationBar = document.getElementById("notification-bar");
    const dismissButton = document.getElementById("notification-dismiss");

    // Add click event listener to the dismiss button
    dismissButton?.addEventListener("click", () => {
        // Hide the notification bar with a fade-out effect
        if (notificationBar) {
            notificationBar.style.transition = "opacity 0.3s ease-out";
            notificationBar.style.opacity = "0";

            // Remove the element after the animation
            setTimeout(() => {
                notificationBar.style.display = "none";
            }, 300);
        }
    });

    // Replace existing scroll behavior with this enhanced version
    const nav = document.getElementById("main-nav");
    let lastScroll = 0;
    const scrollThreshold = 80;

    window.addEventListener("scroll", () => {
        const currentScroll = window.pageYOffset;

        if (currentScroll <= scrollThreshold) {
            nav?.classList.remove("nav-scrolled", "nav-hidden");
            return;
        }

        if (currentScroll > lastScroll) {
            // Scrolling down
            nav?.classList.add("nav-scrolled", "nav-hidden");
        } else {
            // Scrolling up
            nav?.classList.add("nav-scrolled");
            nav?.classList.remove("nav-hidden");
        }

        lastScroll = currentScroll;
    });

    // Add mobile dropdown functionality
    const mobileDropdownButtons = document.querySelectorAll(
        ".mobile-dropdown-button",
    );

    mobileDropdownButtons.forEach((button) => {
        button.addEventListener("click", () => {
            const content = button.nextElementSibling;
            const icon = button.querySelector("svg");

            content?.classList.toggle("hidden");
            icon?.classList.toggle("rotate-180");
        });
    });
</script>
