---
const milestones = [
    { number: "60+", label: "Years in Service", icon: "⚓" },
    { number: "100+", label: "Port Locations", icon: "🌍" },
    { number: "1000+", label: "Completed Deliveries", icon: "🚢" },
    { number: "500+", label: "Satisfied Clients", icon: "🤝" }
];

const specialties = [
    {
        title: "Consistency",
        description: "Delivering reliable service across decades",
        icon: "⭐",
        color: "from-blue-600 to-blue-800"
    },
    {
        title: "Cost Efficiency",
        description: "Providing competitive solutions",
        icon: "💰",
        color: "from-emerald-600 to-teal-800"
    },
    {
        title: "Competency",
        description: "Expert solutions from bridge to propeller",
        icon: "🎯",
        color: "from-indigo-600 to-purple-800"
    }
];

const services = [
    {
        title: "Technical Supply Chain",
        description: "Complete fabrication works, interior carpentry, life raft servicing, and spares supplies",
        image: "https://images.unsplash.com/photo-1471958680802-1345a694ba6d?w=800&auto=format&fit=crop&q=60"
    },
    {
        title: "Supply Chain",
        description: "BRC standard services for all vessel types including passenger, bulk carrier, containers, and tankers",
        image: "https://images.unsplash.com/photo-1494412519320-aa613dfb7738?w=800&auto=format&fit=crop&q=60"
    },
    {
        title: "Crisis Management",
        description: "Expert handling and storage of products with proper safeguarding measures",
        image: "https://images.unsplash.com/photo-1521791136064-7986c2920216?w=800&auto=format&fit=crop&q=60"
    }
];
---

<div class="bg-white">
    <!-- Hero Section -->
    <section class="relative py-20 overflow-hidden">
        <div class="absolute inset-0 bg-gradient-to-br from-blue-50 to-blue-100"></div>
        <div class="absolute inset-0">
            <div class="absolute top-0 right-0 w-1/2 h-1/2 bg-gradient-to-br from-blue-400/10 to-indigo-400/10 rounded-full blur-3xl transform translate-x-1/3 -translate-y-1/3"></div>
            <div class="absolute bottom-0 left-0 w-1/2 h-1/2 bg-gradient-to-tr from-blue-400/10 to-indigo-400/10 rounded-full blur-3xl transform -translate-x-1/3 translate-y-1/3"></div>
        </div>
        
        <div class="relative container mx-auto px-4">
            <div class="max-w-4xl mx-auto text-center">
                <h1 class="text-6xl font-bold text-gray-900 mb-8">
                    Complete Ship Shape Solutions
                    <span class="block mt-4 text-4xl bg-gradient-to-r from-blue-600 to-indigo-600 bg-clip-text text-transparent">
                        From Bridge to Propeller
                    </span>
                </h1>
                <p class="text-xl text-gray-600 leading-relaxed mb-12">
                    Sevenseas, with its strong presence in ship chandling and allied services, 
                    has entered its 6th decade. The legacy continues with the 2nd generation, 
                    accelerating with tailored solutions for worldwide clients.
                </p>
            </div>
        </div>
    </section>

    <!-- Milestones Section -->
    <section class="py-20">
        <div class="container mx-auto px-4">
            <div class="grid md:grid-cols-4 gap-8">
                {milestones.map(item => (
                    <div class="group relative bg-white rounded-2xl p-8 shadow-lg hover:shadow-xl transition-all duration-300 border border-gray-100">
                        <div class="text-4xl mb-4">{item.icon}</div>
                        <div class="text-3xl font-bold text-gray-900 mb-2">{item.number}</div>
                        <div class="text-gray-600">{item.label}</div>
                    </div>
                ))}
            </div>
        </div>
    </section>

    <!-- Specialties Section -->
    <section class="py-20 bg-gray-50">
        <div class="container mx-auto px-4">
            <h2 class="text-4xl font-bold text-center mb-16">What Makes Us Special</h2>
            <div class="grid md:grid-cols-3 gap-12">
                {specialties.map(specialty => (
                    <div class="group relative rounded-2xl overflow-hidden">
                        <div class={`absolute inset-0 bg-gradient-to-br ${specialty.color} opacity-90`}></div>
                        <div class="relative p-8 text-white">
                            <span class="text-4xl mb-6 block">{specialty.icon}</span>
                            <h3 class="text-2xl font-semibold mb-4">{specialty.title}</h3>
                            <p class="text-white/90">{specialty.description}</p>
                        </div>
                    </div>
                ))}
            </div>
        </div>
    </section>

    <!-- Services Section -->
    <section class="py-20">
        <div class="container mx-auto px-4">
            <h2 class="text-4xl font-bold text-center mb-16">Our Services</h2>
            <div class="grid md:grid-cols-3 gap-12">
                {services.map(service => (
                    <div class="group">
                        <div class="relative rounded-2xl overflow-hidden mb-6">
                            <img 
                                src={service.image} 
                                alt={service.title}
                                class="w-full aspect-[4/3] object-cover transition-transform duration-500 group-hover:scale-110"
                            />
                            <div class="absolute inset-0 bg-gradient-to-t from-gray-900/80 via-gray-900/0"></div>
                            <div class="absolute bottom-0 left-0 right-0 p-6">
                                <h3 class="text-xl font-semibold text-white mb-2">{service.title}</h3>
                                <p class="text-white/90 text-sm">{service.description}</p>
                            </div>
                        </div>
                    </div>
                ))}
            </div>
        </div>
    </section>

    <!-- CTA Section -->
    <section class="py-20 bg-blue-900 text-white">
        <div class="container mx-auto px-4">
            <div class="max-w-3xl mx-auto text-center">
                <h2 class="text-4xl font-bold mb-6">Ready to Get Started?</h2>
                <p class="text-xl text-blue-100 mb-8">
                    Contact us for quality, sustained, cost-effective maritime services.
                </p>
                <div class="flex flex-col md:flex-row gap-4 justify-center items-center">
                    <a href="tel:044 4265 7144" class="inline-flex items-center px-8 py-4 bg-white text-blue-900 rounded-full font-semibold hover:bg-blue-50 transition-colors">
                        <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
                        </svg>
                        044 4265 7144
                    </a>
                    <p class="text-blue-200">
                        #10, George Town, Vadamaracoir Street, Chennai-600001
                    </p>
                </div>
            </div>
        </div>
    </section>
</div>
